# Shown on the page heading — non-sensitive, should land in the committed .env
GREETING=Hello from Portainer-Run!

# Name matches the sensitive pattern — should be routed to a Kubernetes Secret
API_KEY=changeme
